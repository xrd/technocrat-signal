hubot:
  build: hubot
  # command: ./bin/hubot --adapter slack
  volumes:
  - ./hubot/hubots:/home/hubot/hubots
  - ./hubot/hubot-utils:/usr/local/hubot-utils
classifier:
  build: classifier
  # command: ruby --version
  ports:
    - "8000:8000"
  command: ruby -rsinatra -e'set :public_folder, "."; set :port, 8000; set :bind, "0.0.0.0"'